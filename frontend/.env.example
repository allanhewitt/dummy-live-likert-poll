# Copy to .env.local for local development.
# Points the frontend at your backend API.

# Local dev (backend running via npm run dev):
VITE_API_BASE=http://localhost:4000

# Once deployed, change to your Coolify backend service URL, e.g.:
# VITE_API_BASE=https://api-poll-test.yourdomain.com
